#pragma once
#include <Jolt/Jolt.h>
#include <Jolt/Physics/Body/BodyID.h>


// ===================================================================================
// ファイル名: JoltHandleComponent.h
// 役割:
// Jolt空間に存在する剛体の「マイナンバー（BodyID）」を保持するシステム用コンポーネント。
//
// 【アーキテクチャ仕様】
// -
// サイズはわずか4バイト。ポインタではなくIDを持つことでキャッシュ効率を最大化する。
//
// 【使い方・ルール】
// - ★絶対に手動で AddComponent しないこと！
// - JoltBodySetupSystem が剛体を生成した証として自動で付与する。
// - PushSystem や PullSystem
// は、このコンポーネントを持つEntityのみを物理同期の対象とする。
// ===================================================================================


// Joltの世界における「マイナンバー」を保持するコンポーネント
// ※ポインタではないため、わずか4バイト。キャッシュ効率が極めて高い。
struct JoltHandleComponent {
    // ※SetupSystemが自動で付与するため、ユーザーが手動で触ることはない。
    JPH::BodyID bodyID;
};